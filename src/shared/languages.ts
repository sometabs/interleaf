// MARC21 bibliographic codes, not ISO 639-1: German is `ger`, not `de`.
// These filter the catalogue and select the matching display edition.
export const READING_LANGUAGES: readonly string[] = ['eng']
